fintech_dbt:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: "{{ env_var('DBT_ENV_SECRET_USER', 'postgres') }}"
      pass: "{{ env_var('DBT_ENV_SECRET_PASS', '1234') }}"
      port: 5432
      dbname: FinTech_Analysis
      schema: public
      threads: 4